# Base raw URL used when generating manifest entry sourceUrl values.
# Defaults to the main branch of this repository if unset.
# Example: https://raw.githubusercontent.com/tm-henningnt/tm-vaibes-content/main
DOCS_REPO_RAW_BASE_URL=

# Target path (relative to repo root) for the generated manifest file.
# Defaults to manifest.json when not provided.
# Example: out/manifest.json
DOCS_REPO_MANIFEST_PATH=
